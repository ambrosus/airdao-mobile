import { prodConfig } from '@constants/bridge-config/bridge.prod.config';
import { testConfig } from '@constants/bridge-config/bridge.test.config';

export const bridgeConfig = {
  prod: prodConfig,
  test: testConfig
};
