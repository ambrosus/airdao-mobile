import { StyleSheet } from 'react-native';
import { scale } from '@utils';

export const styles = StyleSheet.create({
  interval: {
    marginLeft: scale(32)
  },
  lineGraph: {
    width: '100%',
    aspectRatio: 1.5
  }
});
