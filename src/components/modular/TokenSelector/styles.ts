import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  currencySelector: {
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center'
  }
});
