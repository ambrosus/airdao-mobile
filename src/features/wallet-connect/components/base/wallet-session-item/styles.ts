import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 0.5,
    borderRadius: 16,
    borderColor: '#D8DAE0',
    padding: 12
  },
  origin: {
    maxWidth: '65%'
  }
});
