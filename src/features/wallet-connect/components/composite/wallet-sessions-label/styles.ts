import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderRadius: 40,
    borderColor: '#C5EEE1',
    justifyContent: 'center',
    alignItems: 'center',

    paddingVertical: 4,
    paddingHorizontal: 8
  }
});
