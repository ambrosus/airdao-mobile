import { verticalScale } from '@utils/scaling';
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  row: { alignSelf: 'flex-start', marginBottom: verticalScale(24) }
});
