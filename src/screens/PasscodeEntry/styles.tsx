import { StyleSheet } from 'react-native';
import { verticalScale } from '@utils';

export const styles = StyleSheet.create({
  mainFlex: { flex: 1 },
  passcodeText: { paddingTop: verticalScale(160) }
});
